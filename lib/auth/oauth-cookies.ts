/** Short-lived cookies that carry the OAuth handshake between /start and /callback. */
export const OAUTH_STATE_COOKIE = "chroma_oauth_state";
export const OAUTH_VERIFIER_COOKIE = "chroma_oauth_verifier";
export const OAUTH_STATE_TTL_S = 600;
